# hub.gbkz.uk → R2D2 Personal AI Hub (127.0.0.1:8091)
server {
    server_name hub.gbkz.uk;
    client_max_body_size 25m;
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options SAMEORIGIN always;

    location / {
        limit_req zone=perip burst=20 nodelay;
        proxy_pass http://127.0.0.1:8091;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        # WebSocket /hub/live (live-синк погоды/курсов/дайджеста)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    listen [::]:443 ssl; # managed by Certbot
    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/hub.gbkz.uk/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/hub.gbkz.uk/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

}

server {
    if ($host = hub.gbkz.uk) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    server_name hub.gbkz.uk;

    listen 80;
    listen [::]:80;
    return 404; # managed by Certbot


}