# 2026-08-10

## Сессия: настройка личного Telegram-аккаунта + инфраструктура

### Контекст
Дмитрий (пользователь, MacBook-Pro-2) подключает личный Telegram-аккаунт к OpenClaw, чтобы отправлять сообщения и читать диалоги от себя. Ранее исправлял: компакцию, Telegram-бот конфликт, сопряжение iPhone. (Старый сервер/Beget забыт.)

### Что сделано и работает
1. **Конфиг компакции** (`~/.openclaw/openclaw.json` → `agents.defaults.compaction`):
   - `reserveTokens: 20000`, `keepRecentTokens: 24000`
   - `maxActiveTranscriptBytes: "20mb"`, `truncateAfterCompaction: true`
   - `midTurnPrecheck.enabled: true` — защита от ошибки `provider rejected the request schema or tool payload` / `[chat.history omitted: message too large` (возникала при отправке QR-картинки в DeepSeek, который её не принимает).
2. **Telegram-бот**: конфликт поллеров `getUpdates conflict` был из-за пересечения старого/нового шлюза при рестарте. Полный restart шлюза решил. Бот: @Dmbotmy_bot, token/webhook в конфиге.
3. **iPhone**: токен auth `d5f5544774cd049e48adc6b55766e9172ddc3bc3c250fb70`, сопряжение подтверждено (`openclaw devices approve`). Хост Tailscale `hiplet-109548.tail6a4baa.ts.net:443` TLS.

### Личный Telegram-аккаунт (MTProto, Telethon)
- api_id `APIID_PLACEHOLDER`, api_hash `HASH_PLACEHOLDER` (в конфиге `/root/telegram-user-svc/config.json`).
- Номер: `PHONE_PLACEHOLDER`.
- Двухфакторка: сначала пробовали неверный пароль → затем использовался верный 2FA-пароль (значение не хранить в репо, в рантайме; авторизация успешна).
- Сервис: `/root/telegram-user-svc/server.py` (HTTP API на `127.0.0.1:8765`).
  - Endpoint'ы: `/auth/status`, `/auth/start`, `/auth/code`, `/auth/password`, `/send`, `/dialogs`, `/history`.
  - `/send` — отправить (self/другой/группа). `/dialogs` — список. `/history` — читает сообщения диалога за период (days), добавлен в эту сессию.
- **Критично:** сервис теперь **systemd** — `/etc/systemd/system/telegram-user-svc.service`, включён в автозагрузку. Раньше через nohup умирал при рестарте шлюза.
- Тест отправки в Избранное: работает (сообщения с id 90353, 90361, 90362).

### Интеграция с OpenClaw (плагин) — СТАТУС: в процессе
- Плагин `~/.openclaw/plugins/tg-user-tools/` (id `tg-user-tools`) регистрирует `api.registerTool`:
  - `tg_send` — отправить сообщение
  - `tg_dialogs` — список диалогов
  - (план: `tg_history` — для саммари, не добавлен)
- Подключён в `openclaw.json`: `plugins.entries["tg-user-tools"].enabled = true`, путь в `plugins.load.paths`.
- **Проблема:** `openclaw plugins inspect tg-user-tools` показывает `toolNames: []`, `Shape: non-capability`. Контракты `contracts.tools` добавлены в `openclaw.plugin.json` (keys виден) и в `package.json` `openclaw.extensions`, но инструменты не подхватываются рантаймом. Причина не выяснена до конца. `menu-buttons` тоже `non-capability` (это нормально, у него нет registerTool).
- Мой `~/.openclaw/skills/tg_user/skill.json` — **удалить/не использовать**, не подхватывается. Реальные навыки в этой версии — через SKILL.md или плагины.

### Планы пользователя (важно)
Дмитрий хочет **кнопочный флоу «Саммари диалога»**:
- Жмёт кнопку «📊 Саммари» → бот спрашивает контакт → Дмитрий пишет @ник → бот спрашивает период (1д/1нед/1мес) → присылает развёрнутое саммари по пунктам.
- Нужно чтение истории за период (endpoint `/history` готов) + генерация саммари (через LLM DeepSeek, модель по умолчанию `deepseek/deepseek-chat`) + состояние диалога (контакт → период).
- Был вариант свободного текста через агента, но пользователь склоняется к кнопкам + уточнение контакта/периода текстом.

### Технические заметки
- Глупая ошибка: `pkill -f "python3 server.py"` убивал сам процесс выполнения (команда содержит матчащуюся строку). Использовать точный путь или pgrep.
- `send_code_request` через HTTP-worker зависал из-за конфликта asyncio loop при `ThreadingHTTPServer` + `run_until_disconnected`. Решение: `asyncio.run_coroutine_threadsafe` из HTTP-потока в главный loop + `while True` keep-alive в main. Работает.
- SSH-инструкция пользователю: `openclaw config get gateway.auth.token` не работает на Mac (нет openclaw) — команды выполнять на сервере (основной VPS).
- Модель: `deepseek/deepseek-chat` (primary). GPT/`gpt-5.6`, `gpt-5.5` через ProxyAPI (провайдер proxyapi-openai, baseUrl api.proxyapi.ru/openai/v1). Картинки не принимает DeepSeek — для image использовать GPT-4o/Gemini.
- Мульти-FCM/MTProto данные (DC2, ключи) были в сообщении — не сохраняю полностью, только api_id/hash.

### Незавершено
1. Плагин `tg-user-tools` — инструменты не подхватываются (`toolNames: []`), нужно разобраться (возможно манифест/контракты/формат registerTool, или нужен рестарт и проверка через бота).
   - **РЕШЕНО:** инструменты реально работают. `toolNames: []` в `inspect --json` — это статическая метадата команды, не рантайм. Проверено через `openclaw agent`:
     - `tg_send` — агент вызвал, отправил в Избранное, подтвердил "Отправил в Избранное ✅"
     - `tg_history` — агент вызвал, прочитал реальные сообщения
     - `tg_dialogs` — в плагине есть
   - **Правильный формат плагина:** плагин в `~/.openclaw/plugins/tg-user-tools/`, `definePluginEntry` из `openclaw/plugin-sdk/plugin-entry`, `api.registerTool({name, description, parameters, execute})`. Объявить `contracts.tools` в `openclaw.plugin.json` И в `package.json` (`openclaw.contracts.tools`). Подключить: `openclaw.json` → `plugins.entries["tg-user-tools"].enabled=true` + путь в `plugins.load.paths`.
   - Команда теста: `openclaw agent -m "..." --agent main --json`
2. ~~Добавить `tg_history` инструмент в плагин~~ — **ГОТОВО** (добавлен в index.js + контракты)
3. Кнопочный флоу «Саммари диалога» — НЕ нужен отдельно: пользователь выбрал **Вариант 2** (свободный текст через агента). Агент уже делает саммари по пунктам: просит «сделай саммари диалога с X за период» — вызывает tg_history и выдаёт развёрнутое саммари по пунктам (проверено).
4. ~~Проверить, что бот реально отвечает на свободный текст~~ — **ГОТОВО**, работает.
# 2026-08-10

## Сессия: настройка личного Telegram-аккаунта + инфраструктура

### Контекст
Дмитрий (пользователь, MacBook-Pro-2) подключает личный Telegram-аккаунт к OpenClaw, чтобы отправлять сообщения и читать диалоги от себя. Ранее исправлял: компакцию, Telegram-бот конфликт, сопряжение iPhone. (Старый сервер/Beget забыт.)

### Что сделано и работает
1. **Конфиг компакции** (`~/.openclaw/openclaw.json` → `agents.defaults.compaction`):
   - `reserveTokens: 20000`, `keepRecentTokens: 24000`
   - `maxActiveTranscriptBytes: "20mb"`, `truncateAfterCompaction: true`
   - `midTurnPrecheck.enabled: true` — защита от ошибки `provider rejected the request schema or tool payload` / `[chat.history omitted: message too large` (возникала при отправке QR-картинки в DeepSeek, который её не принимает).
2. **Telegram-бот**: конфликт поллеров `getUpdates conflict` был из-за пересечения старого/нового шлюза при рестарте. Полный restart шлюза решил. Бот: @Dmbotmy_bot, token/webhook в конфиге.
3. **iPhone**: токен auth `d5f5544774cd049e48adc6b55766e9172ddc3bc3c250fb70`, сопряжение подтверждено (`openclaw devices approve`). Хост Tailscale `hiplet-109548.tail6a4baa.ts.net:443` TLS.

### Личный Telegram-аккаунт (MTProto, Telethon)
- api_id `APIID_PLACEHOLDER`, api_hash `HASH_PLACEHOLDER` (в конфиге `/root/telegram-user-svc/config.json`).
- Номер: `PHONE_PLACEHOLDER`.
- Двухфакторка: сначала пробовали неверный пароль → затем использовался верный 2FA-пароль (значение не хранить в репо, в рантайме; авторизация успешна).
- Сервис: `/root/telegram-user-svc/server.py` (HTTP API на `127.0.0.1:8765`).
  - Endpoint'ы: `/auth/status`, `/auth/start`, `/auth/code`, `/auth/password`, `/send`, `/dialogs`, `/history`.
  - `/send` — отправить (self/другой/группа). `/dialogs` — список. `/history` — читает сообщения диалога за период (days), добавлен в эту сессию.
- **Критично:** сервис теперь **systemd** — `/etc/systemd/system/telegram-user-svc.service`, включён в автозагрузку. Раньше через nohup умирал при рестарте шлюза.
- Тест отправки в Избранное: работает (сообщения с id 90353, 90361, 90362).

### Интеграция с OpenClaw (плагин) — СТАТУС: в процессе
- Плагин `~/.openclaw/plugins/tg-user-tools/` (id `tg-user-tools`) регистрирует `api.registerTool`:
  - `tg_send` — отправить сообщение
  - `tg_dialogs` — список диалогов
  - (план: `tg_history` — для саммари, не добавлен)
- Подключён в `openclaw.json`: `plugins.entries["tg-user-tools"].enabled = true`, путь в `plugins.load.paths`.
- **Проблема:** `openclaw plugins inspect tg-user-tools` показывает `toolNames: []`, `Shape: non-capability`. Контракты `contracts.tools` добавлены в `openclaw.plugin.json` (keys виден) и в `package.json` `openclaw.extensions`, но инструменты не подхватываются рантаймом. Причина не выяснена до конца. `menu-buttons` тоже `non-capability` (это нормально, у него нет registerTool).
- Мой `~/.openclaw/skills/tg_user/skill.json` — **удалить/не использовать**, не подхватывается. Реальные навыки в этой версии — через SKILL.md или плагины.

### Планы пользователя (важно)
Дмитрий хочет **кнопочный флоу «Саммари диалога»**:
- Жмёт кнопку «📊 Саммари» → бот спрашивает контакт → Дмитрий пишет @ник → бот спрашивает период (1д/1нед/1мес) → присылает развёрнутое саммари по пунктам.
- Нужно чтение истории за период (endpoint `/history` готов) + генерация саммари (через LLM DeepSeek, модель по умолчанию `deepseek/deepseek-chat`) + состояние диалога (контакт → период).
- Был вариант свободного текста через агента, но пользователь склоняется к кнопкам + уточнение контакта/периода текстом.

### Технические заметки
- Глупая ошибка: `pkill -f "python3 server.py"` убивал сам процесс выполнения (команда содержит матчащуюся строку). Использовать точный путь или pgrep.
- `send_code_request` через HTTP-worker зависал из-за конфликта asyncio loop при `ThreadingHTTPServer` + `run_until_disconnected`. Решение: `asyncio.run_coroutine_threadsafe` из HTTP-потока в главный loop + `while True` keep-alive в main. Работает.
- SSH-инструкция пользователю: `openclaw config get gateway.auth.token` не работает на Mac (нет openclaw) — команды выполнять на сервере (основной VPS).
- Модель: `deepseek/deepseek-chat` (primary). GPT/`gpt-5.6`, `gpt-5.5` через ProxyAPI (провайдер proxyapi-openai, baseUrl api.proxyapi.ru/openai/v1). Картинки не принимает DeepSeek — для image использовать GPT-4o/Gemini.
- Мульти-FCM/MTProto данные (DC2, ключи) были в сообщении — не сохраняю полностью, только api_id/hash.

### Незавершено
1. Плагин `tg-user-tools` — инструменты не подхватываются (`toolNames: []`), нужно разобраться (возможно манифест/контракты/формат registerTool, или нужен рестарт и проверка через бота).
   - **РЕШЕНО:** инструменты реально работают. `toolNames: []` в `inspect --json` — это статическая метадата команды, не рантайм. Проверено через `openclaw agent`:
     - `tg_send` — агент вызвал, отправил в Избранное, подтвердил "Отправил в Избранное ✅"
     - `tg_history` — агент вызвал, прочитал реальные сообщения
     - `tg_dialogs` — в плагине есть
   - **Правильный формат плагина:** плагин в `~/.openclaw/plugins/tg-user-tools/`, `definePluginEntry` из `openclaw/plugin-sdk/plugin-entry`, `api.registerTool({name, description, parameters, execute})`. Объявить `contracts.tools` в `openclaw.plugin.json` И в `package.json` (`openclaw.contracts.tools`). Подключить: `openclaw.json` → `plugins.entries["tg-user-tools"].enabled=true` + путь в `plugins.load.paths`.
   - Команда теста: `openclaw agent -m "..." --agent main --json`
2. ~~Добавить `tg_history` инструмент в плагин~~ — **ГОТОВО** (добавлен в index.js + контракты)
3. Кнопочный флоу «Саммари диалога» — НЕ нужен отдельно: пользователь выбрал **Вариант 2** (свободный текст через агента). Агент уже делает саммари по пунктам: просит «сделай саммари диалога с X за период» — вызывает tg_history и выдаёт развёрнутое саммари по пунктам (проверено).
4. ~~Проверить, что бот реально отвечает на свободный текст~~ — **ГОТОВО**, работает.

---

## Сессия 2 (14:00–14:52): ПЕРЕЕЗД НА НОВЫЙ СЕРВЕР + НАСТРОЙКА МОДЕЛЕЙ

### Контекст
Дмитрий переносит OpenClaw на новый сервер (VPS, `vps-7182`, Linux x64, host VM). Теперь я отвечаю уже С НОВОГО сервера. Переезд делала прошлая инстанция; я (текущая сессия) — уже на новом. (Старый сервер/Beget забыт.)

### Инфраструктура нового сервера (проверено)
- **Tailscale**: установлен 1.102.2, УЖЕ в тайлнете `tail6a4baa.ts.net`. Мы = `vps-amnezia` → **100.79.153.33**. В сети: `iphone182` (100.88.9.34, онлайн), Auth-ключ уже ввели (прошлая инстанция).
- **Swap 2GB**: создан, используется ~191M.
- **ufw (файрвол)** — включён и причёсан в этой сессии:
  - SSH (22) открыт, весь tailnet-трафик пропущен (tailscale0)
  - Порт 18789 (device-pair) — теперь **только из tailnet** (100.64.0.0/10), раньше был широко открыт на весь мир
  - Были широко открыты 18789/443/41631 на 0.0.0.0 — **закрыл**
  - Default: deny incoming / allow outgoing. Доступ не отрезан (проверено).
- **Данные перенесены**: workspace, память (8/9/10 авг), Google Calendar (service-account.json + скрипты), OpenRouter ключ (`/root/.openclaw/credentials/openrouter.key`), конфиги/плагины.

### Настройка моделей (ключевое решение, что выбрал пользователь)
Дмитрий сам выбирает модели (не любит, когда я навязываю). Итоговая конфигурация к 14:28:
- **primary (основная):** `deepseek/deepseek-chat` → фактически **DeepSeek V4 Flash** (проверено тестом: на api.deepseek.com id `deepseek-chat` и `deepseek-reasoner` оба алиас на v4-flash). Дёшево, 1M ctx, без rate-limit, код-профи. Баланс прямого API: изначально ¥19.85, в дашборде потрачено ¥14.52, 2295 запросов, ~173M токенов (скрин Дмитрия).
- **imageGenerationModel:** сейчас `qwen/qwen-image-3-pro` (через OpenRouter) — НО СМ. БЛОК ПРОБЛЕМ ниже.
- Провайдеры: deepseek (прямой) · openrouter · google — все на месте.

### Что пробовали/решили по другим моделям (зафиксировать решения!)
1. **Nemotron 3 Ultra** (`nvidia/nemotron-3-ultra-550b-a55b:free`) — бесплатная, ctx 1M. Стояла primary, потом Дмитрий сменил на V4 Flash. Осталась в каталоге OpenRouter как доступная.
2. **Grok Imagine** — на OpenRouter НЕ существует (нет image-gen вообще). Не ставим.
3. **Xiaomi MiMo v2.5** (`xiaomi/mimo-v2.5`) — $0.14/$0.28 in/out, ctx 1.05M. Дмитрий рассматривал, но НЕ выбрал явно. Остался нереализованным хвостом: «докинуть MiMo/Hy3 в каталог?» — без ответа.
4. **Tencent Hunyuan 3 (:free)** (`tencent/hy3-20260706:free`) — по живому API точного id НЕ нашёл (по кешу `/tmp/or_models.json` тоже). Но Дмитрий прислал ссылку — на OpenRouter страница есть. Нереализованный хвост: «проверить живой ли :free». Из топа популярности (скрин Дмитрия): **Hy3 free = №1, 24.7%** доли токенов, DeepSeek V4 Flash №2 (10.8%), MiMo-V2.5 №3 (10%).
5. **DeepSeek V4 Flash** (`~deepseek/deepseek-v4-flash-latest`) — $0.08/$0.16 in/out, ctx 1M. Дмитрий выбрал. Но ТАК КАК прямой API deepseek УЖЕ отдаёт v4-flash — primary логичнее через прямой API (дешевле, баланс есть), так и сделали.

### ✅ СДЕЛАНО в этой части
- ufw причёсан (см. выше).
- primary переведён на `deepseek/deepseek-chat` (= v4-flash через прямой API). Hot reload без ошибок.
- Каталог OpenRouter: убраны DeepSeek V3.1, gpt-4o-mini (vision), Llama-4. Остались: Nemotron :free + qwen-image-3-pro. **ВАЖНО:** gpt-4o-mini был единственной vision-моделью для ЧТЕНИЯ картинок — после удаления чтение скринов недоступно через OR. В этой сессии для чтения скринов использовал gpt-4o-mini (успешно читал график популярности и дашборд deepseek).

### 🔴 НЕРЕШЁННАЯ ПРОБЛЕМА (АКТИВНАЯ НА МОМЕНТ ФЛАША): генерация картинок через qwen-image-3-pro
**Ситуация:** Дмитрий специально оформил OpenRouter-ключ для image-генерации, хочет `qwen/qwen-image-3-pro`. Просил: «Gemini убери с генерации».

**Факты проверены:**
- Модель РЕАЛЬНО работает через `https://openrouter.ai/api/v1/images` (POST → `data[].b64_json`). Прямой curl-тест вернул base64-картинку ✅
- OpenClaw по умолчанию генерит картинки ТОЛЬКО через chat-completions с `modalities:["image","text"]` → для qwen-image-3-pro это 404/не найдена ❌ (проверено дважды: и с `openrouter/`, и с `qwen/` префиксом).
- **Решение (Вариант 2, выбрал Дмитрий):** написать кастомный image-провайдер-плагин, вызывающий `/api/v1/images`.

**Что сделано:**
- Создан плагин `~/.openclaw/plugins/qwen-image-provider/` (index.js + openclaw.plugin.json + package.json). Использует `createOpenAiCompatibleImageGenerationProvider` из `openclaw/plugin-sdk/image-generation` (правильный инструмент для OpenAI-совместимых image-эндпоинтов). `defaultBaseUrl: https://openrouter.ai/api/v1/images`, `providerConfigKey: "models.providers.openrouter"`, модели `qwen/qwen-image-3-pro`. id провайдера: `openrouter-qwen-image`.
- Подключён в `plugins.load.paths` + `plugins.entries`, gateway перезапущен (restart был deferred, потом подхватился — 13 плагинов, включая qwen-image-provider).
- **Ошибка генерации:** `No API key found for provider "openrouter-qwen-image". Auth store: /root/.openclaw/agents/main/agent/openclaw-agent.sqlite`. Т.е. ключ ищется в **пер-агентном auth-хранилище**, а не в глобальном `models.providers.openrouter.apiKey` (фабрика ищет по id провайдера `openrouter-qwen-image`, которого в auth нет).

**Как чинить (порядок auth по доку «config-tools.md:368»):** `auth-profiles.json → env vars → models.providers.*.apiKey`.
- Пробовал: добавил `openclaw.json → models.providers.openrouter-qwen-image.apiKey` — НЕ помогло (генерация в агентском контексте, ключ не виден).
- Пробовал: `openclaw models auth paste-api-key --provider openrouter-qwen-image` (должна писать в пер-агентное SQLite) — результат на момент флаша не подтверждён.
- Дмитрий дважды прислал ключ: OpenRouter-ключ `sk-or-…` (обрезанный; полный в рантайме, в репо не хранить).
- **Открытый вопрос/альтернатива:** проверить формат агентского `auth-profiles.json` (`~/.openclaw/agents/main/agent/`) и записать профиль `openrouter-qwen-image: provider+mode api_key` + положить ключ (keyRef). Либо проверить, что `paste-api-key` реально записал в sqlite, и сделать тест генерации ещё раз.

### Технические заметки/уроки этой сессии
- **Поиск моделей OpenRouter по «её нет» — быть осторожным.** Я трижды ошибался: qwen-image-3-pro «не существует» — но она есть, просто не в `/models` (image-генераторы не отдаются в этом списке; вызываются через `/api/v1/images`). Кеш `/tmp/or_models.json` — битый (фильтр по architecture отсеивает image-модели). Всегда проверять по живой странице/напрямую, а не по кешу.
- **DeepSeek id-алиасы:** на api.deepseek.com `deepseek-chat` и `deepseek-reasoner` оба → v4-flash.
- **deepseek API остаётся как доступный провайдер** (Дмитрий решил) — не выключать.
- **Vite/前置**: ошибка `agents.defaults.model failed` на шлюзе была из-за задания primary/fallback на id, которого ещё нет в каталоге — сначала добавлять модель в каталог, потом ссылаться на неё (это был момент: fallback deepseek v3.1 до актуализации каталога).
- Урок: когда я ставлю модель, сначала `openclaw config set ... models.providers.<id>` или добавить в каталог, потом primary. Защищённые пути (`agents.defaults.model.*`) правятся через `openclaw config set`, не через gateway-патч.
- OpenRouter free-версии имеют rate-limit; платные копеечные ($0.06–$0.27/M). Полностью бесплатных ($0) на OR почти нет (Nemotron :free — да).

### Нереализованные хвосты (для следующей сессии)
1. 🔴 **Добить image-генерацию qwen-image-3-pro** (главный активный хвост, см. блок выше).
2. Проверить результат `paste-api-key` для `openrouter-qwen-image` и тест генерации ещё раз.
3. Хвост по MiMo v2.5 и Hy3 :free — добавить в каталог? (Дмитрий рассматривал, не подтвердил).
4. Хвост: vision-чтение скринов — gpt-4o-mini удалён из каталога; что использовать для чтения картинок, если понадобится снова?
5. Файрвол: проверить, что 18789 device-pair реально работает при подключении новых устройств (после закрытия на весь мир).
