# 2026-08-19

## IP сервера обновлён (по указанию Дмитрия)
- Новый IP основного сервера: **138.124.180.178** (Франкфурт DE).
- Старый 45.95.2.246 — устарел, больше не использовать.
- MEMORY.md уже содержал новый IP (строки про vps-7182 и dmkz.org); устаревший IP нашёлся в `memory/profile.md` — исправлен на 138.124.180.178 + пометка «старый устарел».
- STATE.md — уже актуальный (138.124.180.178).
- Изменения закоммичены и запушены в репозиторий.

## gbkz.uk развёрнут на новом сервере (nginx + TLS)
- Дмитрий сам обновил A-запись gbkz.uk → 138.124.180.178 (прокси Cloudflare включён).
- Установлены nginx 1.24 + certbot 2.9.0; сайт `/etc/nginx/sites-available/gbkz.uk` → прокси на 127.0.0.1:18789 (OpenClaw gateway), WebSocket upgrade для iPhone.
- Сертификат Let's Encrypt: gbkz.uk + www.gbkz.uk, выпущен 19.08, expires **2026-11-17**, автообновление certbot.timer.
- Проверено: https://gbkz.uk → 200, http→https 301, www → 200, WSS-хендшейк 200, трафик от Cloudflare доходит (access log).
- Креды Cloudflare (R2 API) сохранены: `/root/.openclaw/credentials/cloudflare/r2.json` (chmod 600, ВНЕ git). Account id a068ea...; endpoint *.r2.cloudflarestorage.com.
- ufw неактивен на этом сервере (порты открыты по умолчанию).

## Ключи восстановлены (прислал Дмитрий 08:12)
- **OpenRouter** `sk-or-...` → `/root/.openclaw/credentials/openrouter.key` (chmod 600)
- **GitHub token** `ghp_...` → `/root/.openclaw/credentials/github.token`
- **AI Studio (Gemini)** `AQ.Ab8...` → `/root/.openclaw/credentials/ai-studio.key`
- **TG App (MTProto)**: api_id 33037521, api_hash, title "OpenClaw Assistant", short "openclawassist" → `/root/.openclaw/credentials/telegram-app.json` (+ MTProto-серверы DC2 prod/test в чате)
- Все ключи ВНЕ git (в `/root/.openclaw/credentials/`).
- В `openclaw.json` добавлены провайдеры **openrouter** (auto, qwen/qwen-image-3-pro) и **google** (gemini-3.1-flash-image-preview, gemini-2.5-flash) с apiKey. Проверено: `openclaw models list` — 8 моделей, auth yes у всех.
- ⚠️ Схема моделей провайдера НЕ принимает `output` и `input:["text","image"]` — только `input:["text"]` (+ `api`, `contextWindow`, `maxTokens`). Ошибка "Invalid input" на моделях.1/.0 ушла после приведения к схеме deepseek.
- ⚠️ telegram-user-svc (личный TG, MTProto) НЕ перенесён на новый сервер — сервис отсутствует, в хвосты.

## telegram-user-svc развёрнут заново (по команде «да разверни»)
- Исходников на новом сервере не было — написан с нуля: `/root/telegram-user-svc/server.py` (aiohttp, 127.0.0.1:8765; endpoints: /auth/status, /auth/start, /auth/code, /auth/password, /send, /send_file, /dialogs, /history), `config.json` (api_id 33037521, api_hash из telegram-app.json, password пустой), venv + telethon 1.44 + aiohttp 3.14.
- systemd: `/etc/systemd/system/telegram-user-svc.service` (ExecStart=venv/bin/python server.py, Restart=always) — **active**, порт 8765 слушает, /auth/status отвечает.
- ⚠️ **Авторизация НЕ пройдена** (`authorized: false`): нужен вход (номер телефона → /auth/start → код → 2FA-пароль в config.json). В хвосты.
- Скрипт отправки фото: `/root/telegram_user_send_photo.sh <target> <path> [caption]` (POST /send_file; фото ВСЕГДА через /send_file, голый /send = только текст).
- Плагин `~/.openclaw/plugins/tg-user-tools/` создан (index.js: tg_send/tg_send_file/tg_dialogs/tg_history; package.json + openclaw.plugin.json с contracts.tools и configSchema), подключён в `plugins.load.paths` + `plugins.entries.tg-user-tools.enabled`. ⚠️ Загрузку НЕ проверил (validate висел) — проверить при следующем рестарте/сессии.

## Дайджест (11:00, работает)
- `bot_sender.py` падал с HTTP 400 на HTML-разметке → добавлен fallback: при HTTPError отправка повторяется без parse_mode.
- DeepSeek apiKey прописан в `openclaw.json → models.providers.deepseek.apiKey` (взят из auth store sqlite, профиль deepseek:default) — без него AI-саммари в digest.py не работали (ключ искался только в конфиге).
- Дайджест за 2 ч: 19 постов, саммари по источникам, курс ЦБ в конце. ✅

## ⚠️ Хвосты
- R2D2 (11:00): "API provider billing error — ключ без кредитов" — проверить балансы DeepSeek/OpenRouter/Google.
- telegram-user-svc: авторизация (номер/код/2FA).
- Проверить, что плагин tg-user-tools реально загрузился.

## Правило генерации картинок (согласовано с Дмитрием 16:11)
- Генерация С НУЛЯ (без референса) → **бесплатный Pollinations** (`calendar/image_gen.py`, flux). Протестирован 16:11 — работает, файл в `workspace/media/outbox/gen_*.jpg`.
- `image_generate` (OpenRouter→Gemini, ~$1/день) → ТОЛЬКО для редактирования по присланному фото (пёс-леопард, портреты, визитки).
- Записано в MEMORY.md (секция «Картинки»).
# 2026-08-19 (дополнение 18:33 UTC)

## Расходы за день (DeepSeek) + компакция
- Сегодня потрачено **$6.51** (3332 вызова): кэш $4.05 (62%!), вход $1.97, выход $0.50. Причина — раздутый контекст (до ~650K токенов на вызов) из-за пустой `compaction: {}` на новом сервере.
- Настроена компакция в openclaw.json (16:04): `mode: safeguard`, `keepRecentTokens: 200000`, `maxActiveTranscriptBytes: "2mb"`, `truncateAfterCompaction: true`, `midTurnPrecheck.enabled: true` (раньше был false). Hot reload применил, гейтвей active, бэкап сделан.
- Баланс DeepSeek API: **USD 4.07** (is_available), CNY −0.29 (ушёл в минус — оплата идёт по USD).
- Баланс OpenRouter: лимит не установлен (null), расход за месяц $7.24, за сегодня $0.99 (генерации картинок через Gemini-прокси).

## Визитка Сбер (Дружинина Екатерина Александровна, CFA)
- Финальный дизайн (после многих итераций): **лицевая RU** — русский текст как на оригинале, лого «СБЕР»; **оборотная EN** — английский текст (Executive Director / Financing Management for Ferrous and Non-Ferrous Metallurgy... / PJSC Sberbank / 121170, Moscow, Kutuzovsky Ave. 32k2 / T +7 (495) 665 56 00 ext. 14 201 / M +7 (925) 385 73 87 / EkADruzhinina@sberbank.ru / sber.ru), лого «SBER» латиницей, цветной градиентный значок (кольцо+галочка) на обеих сторонах.
- Текст визитки извлечён через Gemini API напрямую (vision-инструмент не работал: модели в каталоге помечены input:["text"]).
- Файлы: `/root/.openclaw/media/business-card-vector.svg/.pdf/.zip`, `business-card-print-hi.pdf` (600 dpi, из присланных фото, 90×50 мм), превью PNG. Лого — стилизованная копия (фирменный вектор Сбера не прикладывался).
- ⚠️ **Урок**: MEDIA-вложения доставляются ТОЛЬКО из `/root/.openclaw/media/` (managed media dir), НЕ из `/root/openclaw/media/`. SVG Telegram не отдаёт как вложение — упаковывать в ZIP. Несколько MEDIA в одном сообщении — работают, но только если каждая строка начинается с MEDIA: без markdown-обёрток.

## Google Calendar: встреча НЕ добавлена (нет кредов)
- Дмитрий просил добавить встречу «Тест проверка» завтра 20.08 в 12:00 — **не выполнено**: на новом сервере нет `/root/.openclaw/credentials/gcal/service-account.json` (не перенесён).
- Обыскал всё: ФС, git-историю, sqlite, сессии, снапшоты, кэши (npm/pip), /root/data, DevDocs, /home, /opt, /srv, bash history, audit — **service-account.json нигде нет**.
- Дмитрий сказал «в бэкапах там и api google workspace есть» — но в бэкапах найден только AI Studio ключ (это Gemini API, для Calendar не подходит). Жду от Дмитрия сам файл service-account.json или уточнение, что за «google workspace api».
- gcal_reader.py на месте, googleapiclient установлен (pip install --break-system-packages google-api-python-client), команда add готова — нужен только файл кредов.

## Портреты Дмитрия (image_generate)
- Несколько итераций «лицо из фото → студийный портрет в белой рубашке» (по 9 фото, потом по 2+2) — модель (gemini-3.1-flash-image-preview) плохо держит точное сходство, рисует «похожего красивого человека». Честно сказал Дмитрию об ограничении. Лучше всего работал режим edit «только переодеть, лицо не трогать» (dmitry-white-shirt-edit — «лучше, но не я»).

## Погода (запрос 11:24)
- Москва: +18°C (ощущается +11), небольшой дождь, ветер ЮЗ 29 км/ч, днём до +21°C, завтра +21°C. wttr.in через web_fetch.

## Google Calendar ПОДКЛЮЧЁН через OAuth (21:54) — встреча добавлена!
- Дмитрий прислал OAuth-клиент (web, client_id 1093906737810-...apps.googleusercontent.com, project claw-506018) — сохранён в `/root/.openclaw/credentials/gcal/oauth-client.json`.
- Проблема была: (1) device-flow не работает для web-клиентов, (2) redirect_uri_mismatch — Дмитрий добавил в консоли Authorized redirect URI **http://localhost:8080/** (сначала с опечаткой locahost — исправлено).
- OAuth-флоу: ссылка авторизации → Дмитрий дал доступ → код из адресной строки → обмен на токены. Токены: `/root/.openclaw/credentials/gcal/tokens.json` (access + refresh, chmod 600).
- ✅ **Встреча «Тест проверка» создана: 2026-08-20 12:00–13:00 Europe/Moscow** (id 5d72faqr34h3mvhfmu6roitchk), проверено чтением через API.
- Доступны календари: Праздники России + dmgromov03@gmail.com.
- ⚠️ gcal_reader.py всё ещё настроен на service-account — теперь доступ через OAuth-токены; при следующей доработке переключить скрипт на tokens.json (или написать oauth-обёртку).
