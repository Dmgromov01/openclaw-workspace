
## 06:53 — Миниапп R2D2 упал (502 Bad Gateway)
- Симптом: скрин Дмитрия — Cloudflare 502, Host Error (gbkz.uk), 06:53 UTC.
- Причина: nginx → /miniapp/ → 127.0.0.1:8080, а python-сервер миниаппа (server.py) умер (запущен вручную nohup, НЕТ systemd-юнита/авторестарта). nginx и гейтвей были живы.
- Фикс: перезапуск `nohup venv/bin/python miniapp/server.py >> miniapp_server.log 2>&1 &` → порт 8080 слушает, /miniapp/ = 200.
- OOM не найден в journalctl; просто умер. Надо завести systemd-юнит для авторестарта (предложено Дмитрию).

## 06:57 — systemd-юнит для миниаппа (по «да» Дмитрия)
- Создан `~/.config/systemd/user/miniapp.service` (копия в services/miniapp.service): python server.py на :8080, Restart=always, RestartSec=5, лог append в miniapp_server.log, enabled.
- Работает: active (running), порт 8080 слушает, /miniapp/ → 200 локально и публично.
- Больше не умрёт молча: автоподъём при падении и при ребуте (linger=yes).

## 07:01 — Календарь в миниаппе не работал (500)
- Симптом: /api/calendar → {"error": "No module named 'google'"} 500.
- Причина: миниапп поднят через venv/bin/python, а google-библиотек в venv НЕ было (system python3 их имел — раньше сервер, видимо, запускался системным python).
- Фикс: `venv/bin/pip install google-api-python-client google-auth google-auth-oauthlib google-auth-httplib2`, restart miniapp.service.
- Проверено: /api/calendar → 200, события отдаются (сегодня «Дегустация вина» 18:00).

## 11:10–11:25 — Clawpatch: установка, ревью, фиксы, рутина
- **Clawpatch** (npm, официальный от openclaw/clawpatch) установлен: clawpatch 0.7.2 + Codex CLI 0.149.0.
- ⚠️ Codex ≥0.100 требует `wire_api="responses"`, chat больше не поддерживает. DeepSeek НАТИВНО поддерживает Responses API (`/v1/responses` → 200, полный ответ) — поэтому свежий Codex + `wire_api="responses"` работает с DeepSeek. Старые версии Codex (0.80) НЕ тянут output_schema (Clawpatch падает с unsupported operation) — не использовать.
- Конфиг: `~/.codex/config.toml` → provider deepseek (base_url https://api.deepseek.com/v1, env DEEPSEEK_API_KEY из /root/.openclaw/credentials/deepseek.key, wire_api="responses", approval_mode="never", sandbox_mode="read-only").
- Ревью воркспейса: 1 фича (Python source root), 4 находки в `update_bot_button.py` (лежит в КОРНЕ, не в bot/):
  1. security high: токен через argv (виден в ps/history) — УБРАНО
  2. test-gap: get_token() без тестов — добавлен test_update_bot_button.py (11 тестов, OK)
  3. bug high: исключения чтения .env/config.json и Bot() вне try/except — ОБЕРНУТЫ
  4. maintainability: хардкод /root/openclaw в путях — сделано script-relative + fallback, get_token(paths=None)
- Все 3 фикса подтверждены revalidate (outcome=fixed), закоммичены (76cf2b1, 32111db, b95f30d).
- Рутина: `services/clawpatch-weekly.sh` (cron: пн 05:00 UTC, review --limit 20 + report --json в .clawpatch/weekly.log). .clawpatch/ добавлен в .gitignore (это state, не исходники).

## 12:04–12:07 — image_generate переключён на google/gemini-3.1-flash-image
- По решению Дмитрия (мощнее flash-lite, цена копеечная): `agents.defaults.imageGenerationModel` = { primary: openrouter/google/gemini-3.1-flash-image, fallbacks: [google/gemini-3.1-flash-image-preview] } (правка напрямую в openclaw.json, бэкап openclaw.json.bak-pre-imagegen-20260821-1204*, restart гейтвея).
- Проверено тестом: генерация идёт через openrouter/google/gemini-3.1-flash-image ✅. Нюанс: quality=low не поддерживается моделью (игнорируется, не ошибка).
- Цена: ~$0.004–0.006/фото (вход ~1030 токенов + выход 1024 токена).

## 13:02–13:05 — Гибридный поиск (Hybrid Search) включён
- Провайдер эмбеддингов: **openai-compatible через OpenRouter** (`openai/text-embedding-3-small`, dim 1536) — OpenRouter умеет /v1/embeddings (проверено: text-embedding-3-small, text-embedding-3-large, BAAI/bge-m3). Цена ~$0.02/M токенов.
- Конфиг: agents.defaults.memorySearch = {enabled, provider: openai-compatible, model: openai/text-embedding-3-small, remote.baseUrl: https://openrouter.ai/api/v1, remote.apiKey: openrouter.key, query.hybrid {enabled, vectorWeight 0.7, textWeight 0.3, candidateMultiplier 4}}.
- Индекс пересобран: 17/17 файлов, 195 чанков, dirty: no, FTS ready. Стоимость пересборки — доли цента.
- ⚠️ Урок: НЕ писать `"model": null` в memorySearch (схема не принимает → конфиг инвалиден → гейтвей не стартует). Это ломало гейтвей 12:59, чинил.
- Gemini-эмбеддинги НЕ завелись: ai-studio.key (OAuth AQ...) → 429 quota, google-api.key (AIza...) → 403 (API не включён). Поэтому OpenRouter.
- Бэкап: openclaw.json.bak-pre-hybrid-20260821-1302*.

## 13:14–13:17 — Web search: Parallel Search (Free) подключён
- Решение Дмитрия: не serper (стоп), не SearXNG; **parallel-free** — бесплатный нативный провайдер без ключей.
- `openclaw plugins install @openclaw/parallel-plugin` → id "parallel" (manifest id ≠ npm-имя).
- Конфиг: tools.web.search = {provider: "parallel-free", maxResults: 5, timeoutSeconds: 30}. Бэкап openclaw.json.bak-pre-parallel-*.
- Проверено: web_search "курс доллара ЦБ" → результаты + excerpts за 1.8s. Гейтвей active, 6 плагинов (google, memory-core, openrouter, parallel, telegram, tg-user-tools).
- ⚠️ Нюанс: active web_search provider ОДИН (parallel-free). SearXNG/DuckDuckGo — переключение одной строкой в tools.web.search.provider.

## 13:25–13:27 — «Поумнение»: память по диалогам + dreaming + pro-fallback
- memorySearch.sources: ["memory", "sessions"] + experimental.sessionMemory → индекс 22/22 файлов, 622 чанка (memory 17/197 + sessions 5/425). Теперь memory_search ищет по ВСЕЙ истории диалогов.
- tools.sessions.visibility: "agent" (для recall несвязанных сессий).
- Dreaming включён (plugins.entries.memory-core.config.dreaming: enabled, cron 0 3 * * * — ночной самоанализ, пишет DREAMS.md + обновляет MEMORY.md).
- model fallback: primary deepseek-v4-flash → fallbacks [deepseek-v4-pro].
- Бэкап: openclaw.json.bak-pre-smart-*.

## 13:40 — SOUL.md переписан (личность по требованиям Дмитрия)
- Твёрдые мнения без «зависит от обстоятельств»; корпоративные правила удалены; запрет «Отличный вопрос/Я с радостью помогу/Конечно»; лаконичность; юмор; прямая правда; мат когда уместен; строка «Будь помощником, с которым тебе действительно хотелось бы поговорить в 2 часа ночи...» дословно в Атмосферу.
- Файл теперь на русском.
