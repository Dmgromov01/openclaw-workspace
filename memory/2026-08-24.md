
## 24.08 12:10 — РОТАЦИЯ BOT TOKEN (Batch 1, шаг 4) ✅
- Причина: активный bot token @Dmbotmy_bot (8675950544) утёк в git-историю (аудит 11:29).
- Дмитрий сделал /revoke + /token в BotFather; старый токен мёртв (getMe → 401), новый работает (getMe ok).
- Обновлено: /root/.openclaw/openclaw.json (channels.telegram.botToken) + /root/.openclaw/.env (TELEGRAM_BOT_TOKEN). Бэкап: openclaw.json.bak-pre-token-rotation-*.
- Рестарт: openclaw-gateway.service + miniapp.service — active, Telegram outbound ok.
- ВАЖНО: НОВЫЙ ТОКЕН НЕ ПИСАТЬ В GIT/ЛОГИ. Хранится только в openclaw.json + .env (оба 600, вне git).
- Вместе с этим закрыты шаги 1-3 Batch 1: ufw active (policy drop, 22/80/443+tailnet), miniapp на 127.0.0.1:8080, git-индекс очищен от .venv/*.db (коммит 2882f79).

## 24.08 12:16 — Batch 2 (Reliability) ✅
- Таймауты: jina-core.js (AbortSignal.timeout 30s) + tg-user-tools (15s) — защита от зависших fetch.
- healthcheck.sh: исправлено «telegram-user-svc не systemd» → systemd-юнит; добавлены проверки ufw + miniapp loopback. Прогон: ВСЕ ИНСТРУМЕНТЫ OK.
- Застрявшая delivery a4fc1e2a (моё exec-error сообщение от 11:18) — удалена из delivery_queue_entries с бэкапом. Очередь: 0 записей.
- 12:18 — рестарт гейтвея (активация таймаутов): 7 plugins загружены, pre-warmed 17ms, порт 18789 OK. Сессии miniapp сброшены (25→0, бэкап сделан).

## 24.08 12:22 — Batch 3 (Cleanup) ✅
- logrotate: /etc/logrotate.d/openclaw-workspace (healthcheck.log, travel/*.log, .clawpatch/weekly.log, miniapp_server.log; weekly×4, compress, copytruncate).
- Удалены в _trash_/batch3-20260824: icloud_calendar.py (календарь=Google), orchestrator.py (мёртв, _collect_news заглушка), backup-crontab-20260820.txt (ссылки на несуществующие скрипты), tmp/cbc_en.py, travel/flight_watch.py (дубль flight_api).
- Cron: убрана строка flight_watch (0 6,18), остался flight_api (10 6,18). Валидация: healthcheck ВСЕ OK, flight_api работает (434,860 ₽ на двоих 26.12).
- Коммит 3e75f45. Отложено: удаление .venv (343M, никем не используется) — требует отдельного approval; miniapp.service использует venv/ (403M) — НЕ трогать.

## 24.08 12:34 — memorySearch ПОЧИНЕН (вариант A) ✅
- Найдено (по вопросу Дмитрия «почему ноль вызовов»): memorySearch.model=openai/text-embedding-3-small НЕ была в каталоге провайдера openrouter (там были только openrouter/auto, qwen-image-3-pro). 21.08 в логах была ошибка «memorySearch.model: Invalid input: expected string, received null». tool memory_search вызывался 0 раз; векторная часть гибрида молча не работала (жили на текстовой 0.3).
- Фикс: добавлена модель openai/text-embedding-3-small в models.providers.openrouter.models (contextWindow 8191, api openai-completions) + рестарт гейтвея.
- Проверка: memory_search вернул vectorScore 0.55/0.48/0.45 (embeddings реально считаются, provider openai-compatible). Бэкап: openclaw.json.bak-pre-embfix-*.
- Вывод: OpenRouter не показывает embedding-модели в GET /models, но POST /v1/embeddings работает.
