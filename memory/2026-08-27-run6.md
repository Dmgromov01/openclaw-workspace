# 2026-08-27 evening — run6 (Grok → GitHub, bot applies on the machine)

Plan and files pushed to `Dmgromov01/openclaw-workspace`. Hub code not touched.

## Owner decision
- Remove exec approvals for agent main (no more /approve buttons).
- Gateway → system unit, linger root, watchdog :18789 and :8765.
- Expand allowlist; outside list → deny without hanging. Not security=full.
- Burn Google from LLM/vision. Photos = DeepSeek Vision. No CLI media.

## Files
- `docs/PROMPT-run6.md` — prompt for the bot (paste whole into @Dmbotmy_bot)
- `docs/run6.md` — why
- `services/openclaw-gateway.service` — system unit template
- `services/loop-watchdog.sh` + `loop-watchdog-cron.sh`

## Exec
- `tools.exec.mode=allowlist` (= security=allowlist + ask=off)
- `channels.telegram.execApprovals.enabled=false`
- allowlist: ls cat head tail df journalctl date uname free zramctl mkdir mv tar gpg git ss curl openclaw
- systemctl/loginctl — only for this run, then drop them

## Do not touch
hub code, ufw, zram, Parallel, Context7, GitHub MCP, hub agent without tools, Mini App.

## Secrets
Morning `memory/2026-08-27.md` contained an iCloud app-specific password in plaintext. Revoke it at appleid.apple.com. Do not copy the password into this file.
