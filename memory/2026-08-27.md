# 2026-08-27

## 🚀 R2D2 Hub развёрнут на hub.gbkz.uk (06:36–06:40)
- Дмитрий добавил A-запись `hub` → 138.124.180.178 в Cloudflare (сначала с опечаткой `hub.gbzk.uk` — имя вводил полным, Cloudflare сделал `hub.gbzk.uk.gbkz.uk`; исправил после разбора скриншота: в Name просто `hub`).
- DNS: hub.gbkz.uk → Cloudflare proxy (172.67.223.229, 104.21.32.152), запись proxied (оранжевое облако).
- nginx: включён `sites-enabled/hub.gbkz.uk` (прокси на 127.0.0.1:8091), конфиг валиден.
- ⚠️ Vite preview блокировал хост: 403 «Blocked request. This host ("hub.gbkz.uk") is not allowed» → добавил `allowedHosts: ["hub.gbkz.uk","www.hub.gbkz.uk","localhost","127.0.0.1"]` в `preview` в `/root/atlas-green-pearl-dawn/vite.config.ts`, перезапуск `systemctl --user restart r2d2-hub.service` → 200.
- ⚠️ certbot был сломан: pyOpenSSL 23.2 + cryptography 50 → `lib.GEN_EMAIL`; починил: `pip3 install --upgrade --break-system-packages pyopenssl` (26.4.0), затем josepy 1.14 несовместим (`X509Req`) → `pip3 install --upgrade --ignore-installed --break-system-packages josepy acme certbot certbot-nginx` (certbot 5.7.0). nginx не в PATH → запуск `PATH=/usr/sbin:$PATH certbot --nginx -d hub.gbkz.uk --redirect`.
- ✅ Итог: HTTPS 200 (tls ok), http→https 301, сертификат Let's Encrypt до **25.11.2026**, автообновление настроено. Edge-TLS на hub.gbkz.uk терминирует Cloudflare (Universal SSL, SAN *.gbkz.uk) — curl показывает валидную цепочку.
- WSS-путь /hub/live в приложении НЕ найден (grep по src/server пусто) → 404 на upgrade; nginx-комментарий в конфиге про live-синк — только намерение.
- Осталось (за Дмитрием): BotFather → Mini App URL = **https://hub.gbkz.uk/**; кнопка в боте (старая ведёт на trycloudflare) — обновить на hub.gbkz.uk при желании.

## 📰 Дайджест (06:11, кнопка)
- Отправлен: Медуза (Цурило 79 лет, Рэтклифф в Москве, бензин), BAZA (криоконсервация), Коммерсантъ (лжеврачи, Непал, вирусная терапия, Кусама, 267 БПЛА). Курс ЦБ: USD 84.28 / EUR 98.29 / CNY 12.53. 14 постов, message_id=10590. RSS РБК отдал ошибку парсинга (no element found) — источник пропущен.

## 10:17–11:06 — AI Personal Hub (R2D2) развёрнут окончательно (ТЗ по OPENCLAW.md)
- Репо atlas-green-pearl-dawn смержен до c92757b («Document bot split, mini-PC deploy, hub watchdog»). npm ci + build, vite preview :8091 за nginx HTTPS → **https://hub.gbkz.uk** (200, title «AI Personal Hub»).
- systemd-юнит `r2d2-hub.service` (EnvironmentFile .env, WorkingDirectory /root/atlas-green-pearl-dawn), watchdog cron раз в минуту через `scripts/hub-watchdog-cron.sh` (wrapper читает .env, не светит секреты в crontab), health :8091, автоперезапуск.
- **Боты разделены** (по OPENCLAW.md): хаб-пейджер = **@HubAlertsbot** (id 8915951913, токен в .env хаба, getMe ok); канал OpenClaw остался на @Dmbotmy_bot (8675950544). Раньше в .env хаба лежал токен @Dmbotmy_bot — заменён.
- **iCloud календарь**: ICLOUD_APPLE_ID=Dmgromov03@gmail.com + app-specific пароль (qoer-kklh-nwqo-zfdy) в .env хаба. Google-календарь (GOOGLE_CALENDAR_*) тоже на месте.
- **Admin-логика изменилась** (в новых коммитах): TELEGRAM_OWNER_ID больше НЕ даёт admin; admin = первый посетитель пустого хаба (`asAdmin = first`). Я очистил БД PGLite (/var/lib/r2d2/pglite) полностью → Дмитрий станет admin, открыв https://hub.gbkz.uk первым (имя → PIN → Настройки → AI-доступ → Проверить агента).
- Чат хаба: OPENCLAW_GATEWAY_URL=http://127.0.0.1:18789 (без /v1), OPENCLAW_MODEL=openclaw/default, gateway.http.endpoints.chatCompletions.enabled=true — проверено: chat/completions 200, ответ «Работаю.». Порт 18789 наружу не публикуется.
- ⚠️ Урок: watch-скрипты с алертами в Telegram не должны читать токен напрямую из crontab — wrapper с `. .env` (set -a) безопаснее.
- Дмитрий подтвердил: «Вроде работает» ✅

## 11:28–11:32 — Шаг 3 (финал): агент hub в OpenClaw
- В openclaw.json `agents.list` = [{"id":"hub", tools.allow:[], skills:[]}], defaults.workspace=/root/openclaw. Gateway перезапущен, подхватил агента hub: текущая Telegram-сессия работает как `agent=hub` ✅
- .env хаба (/root/atlas-green-pearl-dawn/.env) обновлён 11:27 (OPENCLAW_AGENT_ID, GATEWAY_TOKEN/URL, MODEL, PGLITE, TELEGRAM_*). r2d2-hub.service перезапущен → active, local :8091 = 200, https://hub.gbkz.uk = 200 ✅
- Чат хаба через gateway: POST 127.0.0.1:18789/v1/chat/completions (токен из .env, model openclaw/default) → 200, ответ «Да.» — агент hub отвечает ✅
- Осталось за Дмитрием: BotFather Mini App URL = https://hub.gbkz.uk/ (кнопка в боте пока ведёт на trycloudflare).

## 11:38–12:00 — Хардненинг (ТЗ Дмитрия: безопасность+безотказность, без Docker/новых сервисов)
- **ufw**: уже был ок — снаружи только 22/80/443 + tailnet (100.64.0.0/10), default deny. 8091/8080/18789/8765 с WAN закрыты (8091 слушает 0.0.0.0, но ufw режет; loopback/tailnet достаточно).
- **r2d2-hub**: переведён из user-юнита в СИСТЕМНЫЙ (/etc/systemd/system/r2d2-hub.service): Restart=on-failure, RestartSec=5, StartLimitBurst=5, StartLimitIntervalSec=120 (не always — битый билд не уйдёт в цикл). Gateway не трогал (loopback 127.0.0.1:18789 + token).
- **watchdog** (scripts/hub-watchdog.sh + cron wrapper): рестарт ТОЛЬКО после 3 неудачных проверок подряд (FAIL_THRESHOLD=3), URL только http://127.0.0.1:8091/, HUB_RESTART_CMD='systemctl restart r2d2-hub', openclaw-gateway НЕ рестартит, антишторм 1 алерт/10 мин (STORM_SEC=600), алерты через HubAlertsbot → 1916536646. Протестировано: 1-й и 2-й провал — молчание, 3-й — рестарт+алерт.
- **диск**: services/disk-check.sh + cron 35 6 * * * — df / > 85% → алерт в HubAlertsbot, сервисы не рестартит. Сейчас / = 35%.
- **бэкап**: прогнан hub-backup.sh (cron 30 2 * * *), архивы в /var/backups/r2d2: openclaw-state (246MB) + pglite (4.7MB). Пробное восстановление: распаковал pglite в /tmp/hub-restore-test → не пуст (23 элемента) → удалил. Добавлен exclude `.openclaw/media/inbound/credentials*` (в архиве был чужой credentials-файл), пересобран — чисто, .env/credentials в архиве нет.
- **токены**: .env хаба = HubAlertsbot (id 8915951913), TELEGRAM_OWNER_ID=1916536646, токен @Dmbotmy_bot в .env хаба ОТСУТСТВУЕТ (хэши разные). getMe подтвердил.
- **дубли GCal**: из .env хаба убраны ICLOUD_APPLE_ID/ICLOUD_APP_PASSWORD (iCloud устарел, Google — основной; убраны дубли календаря). Бэкап .env.bak-pre-nodup-20260827. Хаб после рестарта 200.
- healthcheck.sh: miniapp теперь планово off → проверки адаптированы (не падает).

## run5: ребут в 6.8.0-138 (12:52 UTC)
- Правки до ребута: bindings telegram->main (+main default:true в agents.list), HEARTBEAT.md чеклист (диск>85% -> сообщение, иначе HEARTBEAT_OK), user r2d2-hub disable+reset-failed (inactive/not-found), system r2d2-hub не тронут (active).
- ПОСЛЕ РЕБУТА проверить и отдать Дмитрию: uname -r (ждём 6.8.0-138), zramctl, swapon --show, free -h, systemctl is-active openclaw-gateway r2d2-hub telegram-user-svc, ss 8091/18789, hub.gbkz.uk=200. Если zram нет — статус systemd-zram-setup@zram0 + наличие zram.ko, swapfile НЕ ставить.

## 15:48 — ПОСЛЕ РЕБУТА run5: проверка (всё зелёное)
- uname -r = **6.8.0-138-generic** ✅ (дождались обновления ядра)
- zram: /dev/zram0 zstd 1.9G, active (systemd-zram-setup@zram0 = active (exited), zram-generator) → swapfile НЕ ставим
- free -h: 3.8Gi RAM, 2.7Gi available, Swap 1.9Gi (256K used)
- openclaw-gateway: **active** — user-юнит (user@0.slice), НЕ system; `systemctl is-active openclaw-gateway` в system-скоупе даёт inactive (юнита там нет) — проверять через cgroup или `systemctl --user`
- r2d2-hub: active; telegram-user-svc: active; miniapp: inactive (планово off)
- порты: 80/443 nginx, 8091 hub (0.0.0.0, ufw режет WAN), 18789 gateway (loopback), 8765 tg-svc (loopback)
- hub.gbkz.uk = 200 (0.12s), 127.0.0.1:8091 = 200
- uptime 2:54 (ребут ~12:56)

## 15:53 — HANDOFF от Дмитрия после фиксации 27.08
- Повторно НЕ ставить: linger, watchdog, маршруты (всё уже зафиксировано 15:48).
- Gateway — по-прежнему **user-юнит** (user@0.slice): проверять через `systemctl --user` / cgroup, в system-скоупе его нет.
- Модели/провайдеров НЕ трогать (маршрут выжжен: deepseek text/pro/vision, image-gen openrouter+gemini, jina embeddings).
