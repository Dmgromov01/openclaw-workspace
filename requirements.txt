# Единый манифест зависимостей воркспейса (Python).
# CI устанавливает ровно этот файл: `pip install -r requirements.txt`.

# --- Тесты ---
pytest

# --- Анализ таблиц (Excel/CSV) ---
pandas
openpyxl
duckdb
tabulate

# --- Telegram ---
aiogram
telethon
requests

# --- Google Calendar ---
google-api-python-client
google-auth
